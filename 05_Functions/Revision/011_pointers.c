#include <stdio.h>
int main()
{
  // int a = 5;
  // int *x = &a; // using '*' to store address// using '&' to use address
  // pointer '*'x stores the address of '&'a // int *x=&a;
  // printf("adrs of 'a' = %p\n", &a);
  // printf("adrs of 'a' = %p\n", x);
  // printf("adrs of 'x' = %p\n", &x);
  // printf("value of address store in 'x' = %p\n", *x); // using '%p' for print the address
  // int m = *x;
  // *x = 7;
  // printf("m = %d\n", m);
  // printf("*x = %d\n", *x);

  return 0;
}