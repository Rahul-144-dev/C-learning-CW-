#include <stdio.h>
#include <math.h>
int main()
{
    int arr[5] = {2, 4, 6, 8, 10};
    
    for (int i = 0; i <= 4; i++)
    {
        printf("arr[%d] = %d\n", i, arr[i]);
    }
    // int i = 0;
    // while (i < 5)
    // {
    //     printf("arr[%d] = %d\n", i, arr[i]);
    //     i++;
    // }
    return 0;
}