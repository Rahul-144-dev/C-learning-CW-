#include <stdio.h>
#include <math.h>
int main()
{
    int a, p, r, n, t;
    printf("enter  principal amount : ");
    scanf("%d", &p);
    printf("enter annual interest rate : ");
    scanf("%d", &r);
    printf("enter number of times that interest is compounded per year : ");
    scanf("%d", &n);
    printf("enter time the money is invested or borrowed for, in years : ");
    scanf("%d", &t);
    
    return 0;
}