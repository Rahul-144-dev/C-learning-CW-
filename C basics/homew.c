#include <stdio.h>
#include <math.h>
int main()
{
    // float n, tm = 40, p = 100;
    // printf("enter number : ");
    // scanf("%f", &n);
    // float avg = (n / tm) * p;
    // printf("%f", avg);
    float x1 = 31, x2 = 32, x3 = 33, x4 = 34,tm=40*4;
    float p = (x1 + x2 + x3 + x4) / tm* 100;
    printf("%f", p);
    return 0;
}